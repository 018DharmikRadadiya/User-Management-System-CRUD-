<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management System</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        body {
            background-color: #020617;
            color: #fff;
        }   


        .update-form {
            max-width: 500px;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #1d9bf0;
            border-radius: 5px;
            background-color: transparent;
            margin-top: 50px;
        }

        .form-group {
            margin-bottom: 15px;
            margin-top: 10px;
        }

        .form-group label {
            display: block;
            color: #1d9bf0;
            margin-bottom: 5px;
            font-weight: bold;
            font-size: 1.3em;
            margin-bottom: 10px;
        }

        .form-group input {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 1.2em;
        }

        .form-title {
            color: #1d9bf0;
            text-align: center;
            margin-bottom: 20px;
            font-size: 2em;
        }

        .submit-btn {
            background-color: #1d9bf0;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
            font-size: larger;
            font-weight: bolder;
            transition: 0.2s;
        }

        .submit-btn:hover {
            background-color: #0e598a;
        }
        footer {
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            height: 59px;
            background-color: #000000;
            border-top: 2px solid #1d9bf0;
            text-align: center;
        }
        #footerLove{
            color: red;
            font-size: 1.3em;
        }
        h3{
            margin-top: 10px;
        }
        .table-header {
            color: #1d9bf0;
            text-align: center;
            margin-bottom: 20px;
            margin-top: 20px;
        }
        .form-group label span{
            color: white;
            font-size: 1em;
        }
    </style>
</head>
<body>
    
    <form class="update-form" method="post" action="/users/<%=IdvData.id%>?_method=PUT">
        <h2 class="form-title">Update User</h2>
        <hr>
        <div class="form-group">
            <label>ID: <span><%=IdvData.id%></span></label>
            <label for="username">Username:</label>
            <input type="text" id="username" name="name" value="<%=IdvData.name%>" required>
        </div>
        <div class="form-group">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required value="<%=IdvData.email%>">
        </div>
        <button type="submit" class="submit-btn">Edit</button>
    </form>

    <footer>
        <div class="footer-content">
            <h3>Made with LOVE <span id="footerLove">&#9829</span> | Created by Dharmik Radadiya </h3>
        </div>
    </footer>
</body>
</html>